<%= form_with(model: assigned_grader, local: true) do |form| %>
  <% if assigned_grader.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(assigned_grader.errors.count, "error") %> prohibited this assigned_grader from being saved:</h2>

      <ul>
      <% assigned_grader.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :user_id %>
    <%= form.text_field :user_id %>
  </div>

  <div class="field">
    <%= form.label :assigned_id %>
    <%= form.text_field :assigned_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
